# 图书馆管理系统 - 快速启动指南

## 环境要求

- **JDK 1.8+**
- **Node.js 14+**
- **Docker Desktop**（用于运行MySQL和Redis）

## 启动步骤

### 1️⃣ 启动数据库（Docker）

```bash
cd library-management-system
.\deploy-docker.bat
```

等待提示显示 **"MySQL 和 Redis 已启动"**

---

### 2️⃣ 启动后端

1. 使用 **IntelliJ IDEA** 打开 `ruoyi-backend` 文件夹
2. 找到 `ruoyi-admin/src/main/java/com/ruoyi/RuoYiApplication.java`
3. 右键运行 → **Run 'RuoYiApplication'**
4. 等待控制台显示 **"启动成功"**

---

### 3️⃣ 启动前端

```bash
cd ruoyi-ui

# 首次运行需要先安装依赖（约5-10分钟）
npm install

# 启动
npm run dev
```

等待显示 **"VITE ready"**

---

### 4️⃣ 访问系统

**管理后台**: http://localhost:80
- 用户名: `admin`
- 密码: `admin123`

**用户端界面**: http://localhost:80/user-library

---

## 常见问题

**Q: Docker启动失败？**
- 确保Docker Desktop正在运行
- 检查3307和6379端口是否被占用

**Q: 后端启动失败？**
- 确保MySQL已完全启动（等待30秒）
- 检查JDK版本是否为1.8+

**Q: 前端启动失败？**
- 确认在`ruoyi-ui`目录下运行命令
- 先执行`npm install`安装依赖

---

**🎉 启动完成！系统已包含172本预装图书数据**
